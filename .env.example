# Frontend configuration
HRT_WEB_ADDR=0.0.0.0:4100

# Backend configuration (internal access only)
HRT_SERVER_ADDR=127.0.0.1:4200

# CORS allowed origins (comma-separated)
# For production, replace hrt.example.com with your actual domain
HRT_ALLOWED_ORIGINS=https://hrt.example.com,http://127.0.0.1:4100