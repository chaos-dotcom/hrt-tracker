services:
  hrt-tracker:
    name: hrt-tracker
    image: ghcr.io/chaos-dotcom/hrt-tracker:latest
    ports:
      - "3000:3000"
    volumes:
      - ./hrt-data.json:/app/hrt-data.json
    restart: unless-stopped
